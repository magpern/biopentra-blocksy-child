## Biopentra Blocksy Child 1.0.0

Initial version-controlled release. Byte-identical import of the theme as it existed on dev.biopentra.eu with no version control and no bind mount — bringing it into this repository closes that gap ahead of the mobile-first storefront redesign.

### Added

- Checkout v2 presentation layer, WooCommerce checkout template overrides, CI/build/release tooling (mirrors [biopentra-loop-card](https://github.com/magpern/biopentra-loop-card) conventions).

### Changed

Nothing — this release has no behavioral or visual effect. See `CHANGELOG.md`.
